/*package com.ecommerce.ecommerce.service.payment;

import com.ecommerce.ecommerce.dto.payment.PaymentCreateDto;
import com.ecommerce.ecommerce.dto.payment.PaymentDto;

public interface PaymentService {
    // Iniciar un nuevo pago
    PaymentDto createPayment(PaymentCreateDto paymentCreateDTO);

    // Confirmar un pago (simula webhook de pasarela)
    PaymentDto confirmPayment(Long paymentId, String externalTransactionId);
}
*/